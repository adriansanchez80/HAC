let
    Source = Excel.CurrentWorkbook(){[Name="Conceptos_W"]}[Content],
    #"Changed Type" = Table.TransformColumnTypes(Source,{{"Index", Int64.Type}, {"ITEMs", type text}, {"Tipo de concepto", type text}, {"Mes ini", Int64.Type}, {"Mes fin", Int64.Type}}),
    #"Valor reemplazado" = Table.ReplaceValue(#"Changed Type",null,1,Replacer.ReplaceValue,{"Mes ini"}),
    #"Valor reemplazado1" = Table.ReplaceValue(#"Valor reemplazado",null,12,Replacer.ReplaceValue,{"Mes fin"}),
    #"Personalizada agregada" = Table.AddColumn(#"Valor reemplazado1", "nº meses prorrateo", each 
if [Mes fin]>[Mes ini] 
              then [Mes fin]-[Mes ini]+1  else
              
        [Mes fin]+(12-[Mes ini])+1),
    #"Filas filtradas" = Table.SelectRows(#"Personalizada agregada", each [Tipo de concepto] <> null and [Tipo de concepto] <> ""),
    #"Tipo cambiado" = Table.TransformColumnTypes(#"Filas filtradas",{{"nº meses prorrateo", Int64.Type}})
in
    #"Tipo cambiado"